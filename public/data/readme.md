# Catalog Data Notes

- `sanmar-catalog.sample.generated.json` remains the UI fallback sample catalog used by the app today.
- `catalog/t-shirts.generated.json` is the first category chunk generated from `SanMar_SDL_N_main_downsize.xlsx`.
- Follow-up PRs will add the remaining chunk files (`hoodies`, `long-sleeve`, `sweatshirts`, `polos`, `bags`, `other`) to keep each diff small and reviewable.
